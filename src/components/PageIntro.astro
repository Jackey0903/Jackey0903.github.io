---
type Props = {
  eyebrow: string;
  title: string;
  copy: string;
};

const { eyebrow, title, copy } = Astro.props;
---

<section class="page-intro reveal">
  <p class="eyebrow">{eyebrow}</p>
  <h1>{title}</h1>
  <p>{copy}</p>
</section>
